Tips:

It takes the "in.png" image from test/ directory and split the lines which can be found in result/. 

"out.png" is the scanning output and "out2.png" is the resizing output.

The resize is set to 50% scaling. To change it, open "main.py" and change the value of scale_percent. The size of the image tends to affect line detection.